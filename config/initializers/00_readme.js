module.exports = function genericInitializer() {
    // Files in this directory are included with require() at startup.

    // Asynchronous initializers use a "done" callback: `function(done) { }`
    // Execution of the succeeding initializer is postponed until the callback
    // is received, ensuring an orderly initialization sequence.
}
