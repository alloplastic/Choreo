// Backing code for the Choreo Player web module

(function ($) { // Block scoping, to keep it out of the global namespace

	// TBD: is this how we should handle client-side communication?
	window.choreo.player = this;

	// TBD: really should define a player class
	window.choreo.player.loadScene = function(sceneData) {
		//alert("scene = " + JSON.stringify(sceneData));
		
	}

	// custom functions

	$(document).ready(function() {

		

	});


})(jQuery);