// The backing code for index.html -- i.e. "home"

(function ($) {

	// window.choreo = {};
	// window.choreo.apiRoot = "stubAPI/";

	// TBD: define a minimal SPA API, e.g.:
	// var result = window.choreo.safeCall('window.choreo.players["game887636464"].addLayer(layer))');
	// (in lieu of a message-passing scheme)

	$(document).ready(function() {

	});

})(jQuery);